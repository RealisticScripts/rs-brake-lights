Config = {
  -- enables detailed client/server console logging
  debug = false,

  -- turns the brake lights off after the vehicle has remained stopped for a random time window
  enableParkEffect = true,
  parkTimerMin = 20, -- seconds
  parkTimerMax = 90, -- seconds

  -- brake lights stay on while the vehicle is slowing/stopped under this speed in MPH
  brakeLightThreshold = 8,
}
